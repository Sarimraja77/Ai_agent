[project]
name = "messenger-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "python-dotenv",
    "requests",
    "langchain-groq",
    "langchain",
]